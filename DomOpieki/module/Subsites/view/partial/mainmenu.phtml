<ul class="nav navbar-nav navbar-right">
    <li><a href="/news"> Aktualności</a></li>
    <?php
    foreach ($this->container as $page) {
        echo '<li>';
        $link = str_replace('href="','href="/',$this->navigation()->menu()->htmlify($page) . PHP_EOL);
        echo $link;
        echo '</li>';
    }
    ?>
</ul>