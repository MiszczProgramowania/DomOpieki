<?php
    $title = $this->translate('Subsites');
    $this->headTitle($title);
?>
<h1><?php echo $this->escapeHtml($title); ?></h1>

<p>
    <a href="<?php echo $this->url('adminSubsites', array('action'=>'add'));?>">Dodaj subsitesa</a>
</p>

<table class="table">
    <tr>
        <th>Tytul</th>
        <th>Opis</th>
        <th>&nbsp;</th>
    </tr>
    <?php foreach ($subsites as $singleSubsites) : ?>
        <tr>
            <td><?php echo $this->escapeHtml($singleSubsites->title);?></td>
            <td><?php echo $this->escapeHtml($singleSubsites->description);?></td>
            <td>
                <a href="<?php echo $this->url('adminSubsites',
                    array('action'=>'edit', 'id' => $singleSubsites->id));?>">Edit</a>
                <a href="<?php echo $this->url('adminSubsites',
                    array('action'=>'delete', 'id' => $singleSubsites->id));?>">Delete</a>
            </td>
        </tr>
    <?php endforeach; ?>
</table>